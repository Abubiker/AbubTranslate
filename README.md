# AbubTranslate

Меню-бар переводчик для macOS. Проблема, которую решает: системный
переводчик Apple встроен только в Safari и Заметки — в остальных
приложениях перевести текст можно только скопировав его в отдельное окно
вручную. AbubTranslate даёт для этого один хоткей из любого приложения.

![platform](https://img.shields.io/badge/macOS-15%2B-black) ![swift](https://img.shields.io/badge/Swift-6-orange) ![license](https://img.shields.io/badge/license-GPL--3.0-blue)

## Установка

[Скачать AbubTranslate.dmg](https://github.com/Abubiker/AbubTranslate/releases/latest/download/AbubTranslate.dmg) — macOS 15+, Apple Silicon.

Открыть образ, перетащить в Applications.

Сборка подписана сертификатом разработчика, не нотаризована. При первом
запуске: Настройки → Конфиденциальность и безопасность → Открыть всё равно.

## Использование

Два способа:

- Выделить текст в любом приложении, нажать `⌥⇧T` — панель откроется с
  переводом.
- Открыть панель кликом по иконке, вставить текст в поле «Оригинал» —
  перевод запускается сам, через 500 мс после последнего ввода.

Клик по иконке панель только открывает, ничего не переводит и не трогает
буфер обмена.

Язык оригинала определяется автоматически (можно задать вручную в панели).
Целевой язык — один, задаётся в настройках; по умолчанию язык системы.
Если оригинал уже на целевом языке — панель предлагает кнопкой перевести на
альтернативный, без автоподмены.

`⇄` — поменять оригинал и цель местами. `🔊` — озвучить перевод. `📋` —
скопировать результат. `↩` — вставить перевод на место исходного выделения
(⌘Z в целевом приложении отменяет, буфер возвращается). `⚙️` — настройки.
Второй хоткей, `⌥⇧Y` — озвучить последний перевод, работает при закрытой
панели. Третий, `⌥⇧S` — OCR: распознать и перевести картинку из буфера
(⌥⇧S или `⇧⌘⌃4` для скриншота, либо перетащить файл на панель — разрешения
на запись экрана не нужно, распознаёт Apple Vision локально). Все хоткеи
настраиваются; занятая другим приложением комбинация не сохраняется,
показывается предупреждение.

Повторный перевод того же текста берётся из локального кэша (90 дней,
до 20 МБ) — мгновенно и без расхода квоты облака; над переводом видно, чем
он получен: `из кэша`, `на устройстве` или `DeepL · 1.9s`.

Правый клик по иконке — меню: перевод скриншота из буфера, настройки, выход.

## Интерфейс

Язык интерфейса — 10 языков: английский, русский, испанский, французский,
немецкий, португальский, китайский, японский, арабский, хинди. При первом
запуске — интерактивный онбординг: выбрать язык перевода, выдать доступ и
настоящим выделением проверить хоткей (шаг засчитывается только по
реальному переводу). Язык системы берётся по умолчанию; если перевода на
него нет — английский (для
языков постсоветского пространства — русский, как более близкий этой
аудитории). Дальше меняется вручную в настройках и применяется сразу, без
перезапуска.

Тема — системная, светлая или тёмная. На macOS 26+ панель и настройки
используют Liquid Glass поверх мягкой цветной подложки; на более ранних
версиях — обычные материалы, без стекла.

## Движки перевода

Настройки → Engine. Один активен за раз.

| Движок | Где работает | Условие |
|---|---|---|
| Apple Translation | на устройстве | встроен, без настройки; 22 языка на момент написания |
| MyMemory | облако | ключ не нужен; 5 000 символов/сутки анонимно, 50 000 с указанной почтой (почта не проверяется, регистрация не нужна — просто ключ лимита) |
| Azure Translator | облако | нужен ключ подписки: portal.azure.com, тариф F0 — 2 млн символов/месяц бесплатно, списать деньги сверх лимита физически нельзя, пока не переключишь ресурс на платный |
| Google Translate | облако | нужен API-ключ: console.cloud.google.com; платно сверх бесплатного лимита, жёсткого барьера как у Azure F0 здесь нет — следить за квотой на стороне пользователя |
| DeepL | облако | нужен API-ключ: developers.deepl.com, бесплатный тир — разовый кредит 1 млн символов (не пополняется, дальше платный Growth) |
| OpenAI-совместимый | облако или локально | нужен базовый URL и модель; ключ — если сервер его требует. Подходит любой сервер с OpenAI-совместимым API: сам OpenAI, прокси, локальная Ollama/LM Studio |
| Yandex Cloud Translate | облако | нужен API-ключ сервисного аккаунта со scope translate (консоль IAM) и привязанная карта — тарификация посимвольно с первого символа; для ключей обычного пользователя дополнительно поле folder ID |
| LibreTranslate | облако или локально | URL инстанса; пусто — публичный libretranslate.com (там ключ обязателен, есть бесплатный тариф с регистрацией). Свой инстанс (docker) работает без ключа |

Apple ничего не отправляет в сеть. Локальный OpenAI-совместимый сервер
(Ollama и подобные на `localhost`) и self-hosted LibreTranslate — тоже.
MyMemory, Azure, Google, DeepL, Yandex и удалённые OpenAI-совместимые и
LibreTranslate-эндпоинты — отправляют переводимый текст на сторонний сервер.

Azure, Google, DeepL, OpenAI, Yandex и LibreTranslate при неудаче (нет
ключа, лимит, пара не поддерживается) автоматически откатываются на
MyMemory — тот же принцип, что и раньше.

OpenAI-совместимые модели по умолчанию шлют поля, отключающие
размышления (DeepSeek `thinking`, Qwen `enable_thinking`, OpenAI
`reasoning_effort`), и потолок `max_tokens`: reasoning-токены — главная
причина «переводит по минуте» на model-серверах. Сервер, который отвергает
неизвестные поля получает автоматический повтор без них. Замер времени
виден в кнопке «Проверить» в настройках.

Движок, ключ/регион Azure, ключ Google, ключ DeepL, настройки OpenAI,
ключ Yandex (и folder ID), адрес/ключ LibreTranslate и почта MyMemory
применяются только по кнопке «Сохранить» в настройках —
остальные поля (языки, хоткеи, тема, автозапуск) применяются сразу.
Причина: смена движка посреди набора ключа не должна перезапускать перевод
по недописанным данным.

Azure и Google собраны строго по официальной документации — живым запросом
не проверены: создание аккаунта с биллингом требует карту, такое действие
приложение за пользователя не выполняет. DeepL по той же причине не
проверен живым запросом, но собран по документации developers.deepl.com,
которая читается напрямую без CAPTCHA. MyMemory проверен реальными
запросами.

### Как получить ключ Azure

1. portal.azure.com → **Create a resource** → поиск «Translator» → создать.
2. Тариф — **F0 (Free)**: 2 млн символов/месяц, физически бесплатно, без
   платного пути, пока сам не переключишь ресурс на **S1**.
3. Карта при регистрации нужна только для верификации личности (Microsoft
   делает $1 холд, который снимается и не списывается).
4. После создания ресурса — **Keys and Endpoint** в боковом меню: там
   `KEY 1` (это ключ) и `Location/Region`.
5. Global-ресурс — регион в приложение можно не вписывать, оставить пустым.

### Как получить ключ Google Translate

1. console.cloud.google.com → создать проект (или выбрать существующий).
2. **APIs & Services → Library** → найти «Cloud Translation API» → **Enable**.
3. Нужен биллинг-аккаунт с картой — без него включить платный API нельзя,
   бесплатный лимит здесь не отдельный тариф, а месячный кредит против
   аккаунта с оплатой.
4. **APIs & Services → Credentials → Create Credentials → API key**.
5. Ключ можно (и стоит) ограничить в настройках credential — разрешить
   только Cloud Translation API, чтобы утечка ключа не открывала доступ ко
   всему проекту.
6. Здесь нет F0-аналога — потратил бесплатный лимит, платит карта. Следить
   за расходом: **Billing → Budgets & alerts** в консоли.

### Как получить ключ DeepL

1. developers.deepl.com → зарегистрировать free-аккаунт (карта не нужна
   для free-тира).
2. **API Keys** в личном кабинете → создать ключ.
3. Free-ключ отличается суффиксом `:fx` (например `279a...42a0:fx`) — по
   нему приложение само выбирает бесплатный эндпоинт.
4. Бесплатный лимит («Developer») — разовый кредит 1 млн символов, не
   пополняется помесячно; кончился — либо платный Growth, либо стоп.
5. Кнопка «Проверить лимит» в настройках показывает остаток кредита, не
   выходя из приложения.

### Как настроить OpenAI-совместимый движок

Работает с любым сервером, который отвечает по OpenAI-совместимому API —
это и сам OpenAI, и прокси, и локальные Ollama/LM Studio.

1. **Базовый URL** — например `https://api.openai.com/v1` или
   `http://localhost:11434/v1` для Ollama. Хвост `/chat/completions`
   дописывается сам, вводить его не нужно.
2. **Модель** — идентификатор в том виде, в каком его ждёт сервер
   (`gpt-4o-mini`, `llama3.1`, и т.п.).
3. **Ключ** — обязателен для удалённых серверов. Для локальных
   (`localhost`, `127.0.0.1`, `::1`) можно оставить пустым: заголовок
   авторизации тогда просто не отправляется.
4. Кнопка «Проверить соединение» в настройках делает пробный перевод и
   говорит, отвечает ли сервер выбранной моделью.

MyMemory ключа не требует вообще — просто оставить почту пустой или
указать любую для более высокого лимита.

Офлайн-движка на базе локально скачиваемых нейросетей в приложении нет —
единственный известный источник CoreML-моделей перевода их не публикует,
скачивание было обречено с самого начала.

## Разрешения

Перевод выделенного текста требует Универсальный доступ (Настройки →
Конфиденциальность и безопасность → Универсальный доступ). Механизм:
приложение читает выделение через Accessibility API — буфер обмена не
трогается вовсе. Если приложение выделения через AX не отдаёт (типично для
Electron), оно отправляет синтетический `⌘C`, читает результат и возвращает
прежнее содержимое буфера обмена. Без разрешения — вставка текста в панель
вручную и OCR с картинки.

Разрешение выдано, но не работает: осталась запись от прошлой сборки с
другой подписью. Удалить кнопкой `−` из списка и добавить заново, либо:

```bash
tccutil reset Accessibility com.opensource.abubtranslate
```

## Лицензия

**GPL-3.0-only** — полный текст в [LICENSE](LICENSE).

Свободно и навсегда: можно использовать, изучать, менять и распространять —
при одном условии. Любая распространяемая производная версия (форк, «улучшенная
сборка», перепродажа бинаря) обязана оставаться под GPL-3.0 и нести исходники.
Переименовать проект и закрыть его в коммерческий продукт «как есть» нельзя —
так и задумано. Копирайт на оригинал (© Dmitry Marchenko) сохраняется.

**Нужно по-другому?** Если GPL-условия вам не подходят (например, хотите
встроить код в закрытый продукт), напишите автору — через
[Issues](https://github.com/Abubiker/AbubTranslate/issues) или почту из коммитов.
Для коммерческого использования без копилефта выдаётся отдельная платная
лицензия (dual-licensing, модель Qt/MySQL).

---

<details>
<summary><b>Сборка из исходников</b></summary>

macOS 15+, Xcode 16+, [xcodegen](https://github.com/yonaskolb/XcodeGen).

```bash
brew install xcodegen
security find-identity -v -p codesigning   # найти свою identity
export ABUBTRANSLATE_SIGN_IDENTITY="Apple Development: ваш@email (TEAMID)"
./Scripts/build.sh
```

Результат: `/Applications/AbubTranslate.app` (скрипт сам ставит).
`Scripts/build.sh` — единственный правильный способ собрать проект, **даже
если собирает не эта сессия, а другой инструмент/агент**: голый
`xcodebuild` без ручной пересборки подписи подписывает ad-hoc, а это ломает
не только выданный Accessibility-грант, но и Keychain-доступ к уже
сохранённым API-ключам (Azure/Google/DeepL/OpenAI) — designated requirement
у ad-hoc это хеш бинарника (cdhash), он меняется на каждой пересборке,
macOS видит «другое приложение» и не даёт добраться до старых секретов.
Подробности — в комментарии в начале скрипта.

```
ad-hoc:      designated => cdhash H"3b95b95e…"
сертификат:  designated => identifier "com.opensource.abubtranslate"
                           and anchor apple generic
                           and certificate leaf[subject.CN] = "Apple Development: …"
```

У ad-hoc требование прибито к хешу бинарника — любая пересборка ломает и
выданное разрешение, и доступ к Keychain. У сертификата требование
привязано к нему самому и переживает пересборки.

Тесты чистой логики (выбор направления перевода, нарезка текста под лимит
запроса):

```bash
swiftc -o /tmp/selfcheck Sources/Managers/TranslationDirection.swift \
    Sources/Managers/TextChunker.swift Tools/SelfCheck.swift && /tmp/selfcheck
```

### Структура

```
Sources/
├── TranslatorApp.swift            # @main, статус-итем, поповер, меню правого клика
├── AppModel.swift                 # состояние, движки, оркестрация перевода
├── Views/
│   ├── PanelView.swift             # основная панель
│   ├── SettingsView.swift          # языки, движки, хоткеи, тема
│   ├── HotkeyRecorder.swift        # запись комбинации
│   └── DesignTokens.swift          # шкала размеров/отступов/цветов
├── Managers/
│   ├── SelectionReader.swift       # выделенный текст через синтетический ⌘C
│   ├── TranslationDirection.swift  # выбор альтернативного языка (чистая логика)
│   ├── TextChunker.swift           # нарезка под лимит запроса облачных сервисов
│   ├── TranslationProvider.swift   # протокол провайдера, EngineMode
│   ├── Providers/                  # MyMemory, Azure, Google, DeepL, OpenAI
│   ├── CloudTranslator.swift       # HTTP-клиент MyMemory
│   ├── KeychainHelper.swift        # хранение токена/почты
│   ├── ClipboardManager.swift      # NSPasteboard
│   ├── LanguageDetector.swift      # NLLanguageRecognizer
│   ├── SpeechManager.swift         # AVSpeechSynthesizer
│   └── HotKeyManager.swift         # Carbon RegisterEventHotKey
├── {en,ru,es,fr,de,pt,zh,ja,ar,hi}.lproj/   # локализация, 10 языков
└── Assets.xcassets/
```

Новая строка в интерфейсе — это ключ во всех десяти `.lproj`, а не только
в `en`/`ru`: набор ключей в файлах должен совпадать один в один, иначе на
части языков вылезет сырой ключ вместо текста. Новый язык — ещё и код в
`knownRegions` в `project.yml`, иначе xcodegen не положит `.lproj` в бандл.

</details>
